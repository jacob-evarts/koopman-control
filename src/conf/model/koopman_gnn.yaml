hidden_size: 128
lr: 1e-3
latent_dim: 32
activation: relu
num_gnn_layers: 2
beta: 1.0
# true positions at decode let each cluster get distinct recon (required for spatial plots)
decode_with_pos: true
include_control: true
latent_mode: per_type
num_populations: 2
type_feature_start: 7
latent_dim_per_type: 16
