defaults:
  - _self_
  - model: koopman_ae
  - trainer: default
  - optim: optuna

dataset:
  batch_size: 64
  data_dir: ./data/firefly_synchronization_sweeps
  csv_file: 

save_dirs:
  figures: ./figures
  
wandb:
  project: koopman-control
  mode: disabled