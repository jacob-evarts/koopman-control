defaults:
  - _self_
  - model: koopman_ae
  - trainer: default
  - optim: optuna

dataset:
  batch_size: 64
  dataset_name: arcade
  data_dir: ./data/arcade_sweeps
  csv_file: square_emergent.csv

save_dirs:
  figures: ./figures
  
wandb:
  project: koopman-control
  mode: disabled