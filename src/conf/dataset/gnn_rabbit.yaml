dataset_name: gnn_rabbit
format: gnn_h5
data_dir: ./data/networks/gnn_dataset
dataset_file: gnn.h5
manifest_file: manifest.json
# GNN graph batches are memory-heavy on MPS; keep workers at 0 to avoid semaphore leaks on macOS.
batch_size: 8
num_workers: 0
preload: true
